//Método match
const match = "A capacidade máxima de 100 pessoas já foi atingida".match(/\d+/);

const match2 = "A capacidade máxima de pessoas já foi atingida".match(/\d+/);

console.log(match)
console.log(match2)
