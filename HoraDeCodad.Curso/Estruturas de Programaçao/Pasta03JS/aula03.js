//Nomes revervados para variaveis

/*
Em JavaScript, nomes reservados (ou palavras-chave) são identificadores que não podem ser usados para nomear variáveis, funções, classes ou qualquer outro elemento no código, pois já têm um significado específico na linguagem.

🔒 Palavras-chave reservadas no JavaScript
Estas palavras são reservadas atualmente e não podem ser usadas como identificadores:



break       case        catch       class       const
continue    debugger    default     delete      do
else        enum        export      extends     false
finally     for         function    if          import
in          instanceof  new         null        return
super       switch      this        throw       true
try         typeof      var         void        while
with        yield
*/